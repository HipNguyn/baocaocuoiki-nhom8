<%- include('partials/header') %>

<div class="max-w-2xl mx-auto my-16 bg-white p-8 rounded-2xl shadow-sm border border-red-200 text-center">
    <div class="w-20 h-20 bg-red-100 text-red-600 rounded-full flex items-center justify-center mx-auto mb-6 text-4xl">
        <i class="fa-solid fa-triangle-exclamation"></i>
    </div>
    
    <h1 class="text-2xl font-bold text-slate-800 mb-4">Ối! Đã xảy ra lỗi</h1>
    
    <div class="text-slate-600 mb-8 bg-red-50 p-4 rounded-lg font-mono text-sm border border-red-100 text-left">
        <strong>Chi tiết lỗi:</strong> <br>
        <%= typeof message !== 'undefined' ? message : 'Lỗi hệ thống không xác định.' %>
    </div>
    
    <a href="/index" class="px-6 py-3 bg-slate-800 text-white font-bold rounded-xl hover:bg-slate-900 transition shadow-md">
        <i class="fa-solid fa-house mr-2"></i> Quay về trang chủ
    </a>
</div>

<%- include('partials/footer') %>